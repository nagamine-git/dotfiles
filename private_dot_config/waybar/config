{
  "layer": "top",
  "position": "top",
  "height": 28,
  "margin": "6 6 0 6",
  "spacing": 4,
  "radius": 8,
  "font": "Firge35Nerd Console",

  "modules-left": ["hyprland/workspaces", "hyprland/window"],
  "modules-center": ["clock"],
  "modules-right": ["tray", "temperature", "cpu", "memory", "custom/disk", "network", "pulseaudio", "battery", "custom/power"],

  "hyprland/workspaces": {
    "format": "{icon}",
    "format-icons": {
      "active": "●",
      "default": "○"
    },
    "on-click": "hyprctl dispatch workspace {name}",
    "tooltip": true
  },

  "hyprland/window": {
    "max-length": 40
  },

  "clock": {
    "format": "{:%y.%m.%d %H:%M:%S}",
    "timezone": "Asia/Tokyo",
    "interval": 1,
    "tooltip": true
  },

  "cpu": {
    "interval": 3,
    "format": " {icon}",
    "format-icons": ["⡀", "⣀", "⣄", "⣤", "⣦", "⣶", "⣷", "⣿"],
    "states": {
      "warning": 80,
      "critical": 95
    },
    "tooltip": true
  },

  "memory": {
    "interval": 5,
    "format": "󰆻 {icon}",
    "format-icons": ["⡀", "⣀", "⣄", "⣤", "⣦", "⣶", "⣷", "⣿"],
    "states": {
      "warning": 80,
      "critical": 90
    },
    "tooltip": true
  },

  "custom/disk": {
    "format": "󰋊 {}",
    "exec": "df -h / | awk 'NR==2 {perc=substr($5, 1, length($5)-1); if (perc < 13) {icon=\"⡀\"} else if (perc < 25) {icon=\"⣀\"} else if (perc < 38) {icon=\"⣄\"} else if (perc < 50) {icon=\"⣤\"} else if (perc < 63) {icon=\"⣦\"} else if (perc < 75) {icon=\"⣶\"} else if (perc < 88) {icon=\"⣷\"} else {icon=\"⣿\"}; printf \"%s\", icon}'",
    "exec-if": "exit 0",
    "interval": 30,
    "tooltip": true,
    "tooltip-format": "ディスク使用量: {}"
  },

  "temperature": {
    "interval": 5,
    "format": "󰔏 {icon}",
    "format-icons": ["⡀", "⣀", "⣄", "⣤", "⣦", "⣶", "⣷", "⣿"],
    "warning-threshold": 75,
    "critical-threshold": 85,
    "tooltip": true
  },

  "network": {
    "format-wifi": "󰤨 {essid}",
    "format-ethernet": "󰈀",
    "format-linked": "󰈁 {ifname}",
    "format-disconnected": "󰤮",
    "tooltip-format": "{ifname}: {ipaddr}/{cidr}",
    "tooltip-format-ethernet": "󰈀 {ifname}: {ipaddr}/{cidr}\n󰕒 {bandwidthUpBits}\n󰇚 {bandwidthDownBits}",
    "interval": 10
  },

  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {time}",
    "format-charging": "󰂄 {time}",
    "format-plugged": "󰚥",
    "format-alt": "{icon} {capacity}%",
    "format-time": "{H}:{M}",
    "format-icons": ["󰁺", "󰁼", "󰁾", "󰂀", "󰂂", "󰁹"],
    "interval": 30,
    "tooltip": true,
    "tooltip-format": "バッテリー: {capacity}%\n残り時間: {time}"
  },

  "pulseaudio": {
    "format": "{icon}",
    "format-muted": "󰝟",
    "format-icons": {
      "headphones": "󰋋",
      "default": [
        "󰕿  ",
        "󰖀 ⡀",
        "󰖀 ⣀",
        "󰖀 ⣄",
        "󰖀 ⣤",
        "󰕾 ⣦",
        "󰕾 ⣶",
        "󰕾 ⣷",
        "󰕾 ⣿"
      ]
    },
    "on-click": "pavucontrol",
    "interval": 1,
    "tooltip": true,
    "tooltip-format": "音量: {volume}%"
  },

  "custom/power": {
    "format": "⏻",
    "tooltip": true,
    "on-click": "wlogout -p layer-shell"
  }
}
