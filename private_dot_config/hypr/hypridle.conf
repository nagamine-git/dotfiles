general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

listener {
    timeout = 300                         # 5分
    on-timeout = loginctl lock-session    # ロック
}

listener {
    timeout = 330                         # 5.5分
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on && sleep 0.5
}

# 単一の sleep 判断: 30 分 AFK で idle-sleep.sh が呼ばれる。
# 中身はシンプルな時刻ベース (09:00-22:59 は寝ない, 夜間は条件次第で suspend)。
# 詳細は ~/.local/bin/idle-sleep.sh を参照。
listener {
    timeout = 1800                        # 30分
    on-timeout = ~/.local/bin/idle-sleep.sh
}
