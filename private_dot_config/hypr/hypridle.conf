general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

listener {
    timeout = 300                         # 5分
    on-timeout = loginctl lock-session    # ロック
}

listener {
    timeout = 330                         # 5.5分
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on && sleep 0.5
}

# 2026-08-26: AFK suspend (idle-sleep.sh 呼び出し) を恒久的に廃止。
# ユーザー明示指示により、room-telemetry の「夜間スリープ継続が正解」判断を上書き。
# idle-sleep.sh 自体は削除せず ~/.local/bin/ に残す (ロック/dpms off は上の listener
# で引き続き効く。suspend/hibernate だけ止める)。
