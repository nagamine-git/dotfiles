[Unit]
Description=claude-cpu-watch を 60 秒ごとに実行

[Timer]
# ログイン直後は claude の起動処理で CPU が出るため、少し置いてから基準を取る
OnStartupSec=90
OnUnitActiveSec=60
AccuracySec=5s
# 寝てた間の miss を補填しない (復帰直後にまとめて走られても意味が無い)
Persistent=false

[Install]
WantedBy=timers.target
