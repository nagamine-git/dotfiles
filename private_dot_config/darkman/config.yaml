cat > ~/.config/darkman/config.yaml << 'EOF'
# 位置情報設定（東京近辺の緯度と経度）
location:
  latitude: 35.68
  longitude: 139.76

# これを使わない場合はGeoClueを使用します（サービスが必要）
# use_geoclue: true

# テーマ切り替え時の通知を表示するか
notify: true

# クライアント側でこの設定をオーバーライドできるようにするか
allow_client_overrides: true
