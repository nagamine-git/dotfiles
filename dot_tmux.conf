set -g mouse on

setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

unbind C-b

# C-aにバインド
set-option -g prefix C-t
bind-key C-t send-keys C-t
# コピーモードはvimに似せる
unbind [
bind-key v copy-mode


# painサイズ切り替え
bind-key -n M-Left resize-pane -L 3
bind-key -n M-Right resize-pane -R 3
bind-key -n M-Up resize-pane -U 3
bind-key -n M-Down resize-pane -D 3

bind-key -n M-Tab switch-client -l

set -g @plugin 'tmux-plugins/tpm'
# Hybrid theme palette for tmux-power
set -g @tmux_power_theme '#6e90b0'
set -g @tmux_power_g0 "#161719"
set -g @tmux_power_g1 "#1e1f22"
set -g @tmux_power_g2 "#2a2e33"
set -g @tmux_power_g3 "#434548"
set -g @tmux_power_g4 "#5a626a"
set -g @tmux_power_date_format '%y.%m.%d'
set -g @tmux_power_time_format '%R'
set -g status-style 'bg=#161719,fg=#b7bcba'
set -g message-style 'bg=#6e90b0,fg=#161719'
set -g message-command-style 'bg=#7fbfb4,fg=#161719'
set -g pane-border-style 'fg=#2a2e33'
set -g pane-active-border-style 'fg=#6e90b0'
set -g @plugin 'wfxr/tmux-power'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @tmux-fzf-launch-key 'f'
set -g @plugin '2kabhishek/tmux-tilit'
set -g @plugin 'long-910/claude-tmux-status'
set -g @claude-tmux-auto-status "false"
run '~/.tmux/plugins/tpm/tpm'
set -ag status-right "#[fg=#7fbfb4,bg=#6e90b0]#[fg=#161719,bg=#7fbfb4,bold] ☁ #(claude-usage-colored)#[fg=#b7bcba,bg=#161719]"


# Override tmux-tilit's extrakto binding (extrakto not installed)
bind-key -n M-f run "tmux split-window -l 12 'zsh -ci tmux_fzf_window'"
