# tool-radar 2026-09-14 (期間 2026-09-13 → 2026-09-14)

検出 15件 / 取込価値 3件

## 今日の結論

Claude Code が v2.1.270 → v2.1.271(9/14)へ更新され、今日は久々にまとまった実利ある新機能が来た。特に「Bash/PowerShell/Monitorへのコマンド単位`allowed_domains`(autoモード・サンドボックス化)」は、`defaultMode: "auto"` を全面採用している当環境で今までネットワーク接続先を一切制限していなかった設計上の穴を初めて塞げる機能で、今回のトップ推奨とした。それ以外のA(開発環境)/C(WM・OS)/D(業務SaaS)/E(ハード・インフラ)は無風で、freee-mcp・ROCm・Hyprland・Tailscaleいずれも既出状況から変化なし。

## 推奨アクション (最大3)

1. **Claude Code v2.1.271 — Bash/PowerShell/Monitor へのコマンド単位 `allowed_domains`(autoモード・サンドボックス化)** ★★★ [確認済] — autoモードで実行されるBash/PowerShell/Monitorコマンドについて、そのコマンドが必要とする接続先ホストだけをコマンド単位でレビュー・許可し、それ以外のホストへの通信は拒否できる新機能。既存: `dot_claude/private_settings.json.tmpl` は `"defaultMode": "auto"` を採用しているが `permissions` には `deny` リスト(コマンド単位のブロック)しかなく、ネットワーク接続先そのものを制限する仕組みが今まで存在しなかった空いてる穴。MCP経由(Slack/Playwright等)や汎用Bashコマンドが任意の外部ホストへ到達できる状態を、コマンドごとの許可リストで絞り込める。移行コスト: 低〜中 — 既存動作は変えず追加設定のみだが、実際に効かせるには利用中のBashコマンド/MCPが必要とするドメインを洗い出して`allowed_domains`を書く手間がかかる。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.271 — agent frontmatterへの `omitClaudeMd` 追加** ★★ [確認済] — カスタム/プラグインsubagentを、user/project/local各`CLAUDE.md`を読み込まずに実行できるオプション(managed policyファイルは引き続き読み込む)。既存: `dot_claude/agents/{implementer,researcher,reviewer}.md` の3 agentは、`dot_claude/CLAUDE.md`の「モデル配車則」で個別にmodelをfrontmatterに明示する運用のため、実装・調査・レビュー判定のタスク自体には全社ルールのCLAUDE.md本文を毎回読み込ませる必然性が薄い。コンテキスト量・キャッシュ効率を削れる空いてる穴。移行コスト: 低 — 各agent frontmatterに1行追加するだけ、ただしCLAUDE.md記載ルールを暗黙前提にしている挙動が無いか要確認。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.271 — `claude plugin install`/`update` に `--accept-command <sha256>` 追加** ★★ [確認済] — `--json`実行時に表示されたコマンドのハッシュを指定して、そのコマンドだけを厳密に承認するオプション(`-y`の無条件承認に代わる選択肢)。既存: `dot_claude/private_settings.json.tmpl` の `extraKnownMarketplaces` で外部リポジトリ製 `freee-mcp@freee-mcp-marketplace` を有効化中。8/28・9/2・9/9・9/10・9/11と続いてきたプラグイン/マーケットプレイス関連セキュリティ強化の延長線上にあり、プラグイン更新時に無条件`-y`ではなくハッシュ固定承認へ切り替えられる。移行コスト: 低 — 更新スクリプトを自動化する場合のみ有用、対話的更新なら緊急性なし。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.271 — Remoteセッション(クラウド/self-hosted runner)へのファストモード追加** ★★ [確認済] — ホスト側のファストモード設定または`/fast`がRemoteセッションにも適用可能に(組織が許可する範囲)。既存: `"remoteControlAtStartup": true` を設定中で、本radar自体もリモート実行セッションとして動いているため関連するが、個人アカウントでの適用条件が不明なため次点扱い。
- **freee-mcp v0.35.1 最新版確認** ⌀ [確認済] — GitHub Releases直接確認、9/8以降の新規リリースなし。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然 `rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし。
- **Tailscale最新版** ⌀ [推定] — TS-2026-011修正・Go 1.26.6更新の内容は9/9〜13時点と同一、実機`tailscale version`確認は依然宿題のまま。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/14時点で未発表、直近は8月リリースのまま。
- **freee顧問先管理 | AIエージェント 9/15正式提供開始** ⌀ [確認済] — 明日に迫る。税理士事務所向け機能のため一人法人・自社利用の当環境には引き続き非該当。
- **1Password CLI 2.39.0** ⌀ [確認済] — secret読み取り高速化等の通常更新。`permissions.deny`で`op item*`/`op read*`等を全面ブロック中のため実害なし。
- **Codex CLI rust-v0.154.0(GPT-5.6 Terra既定化)/Antigravity Documentsセクション追加** ⌀ [確認済] — 競合CLIの通常アップデート、乗り換え動機なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/telescope.nvim/nvim-treesitter/lazygit/jujutsu/zellij/sheldon/tmuxプラグイン群とも9/14固有の新着確認できず。
- B (AIエージェント): Claude Code v2.1.271に実利ある変更4系統(上記参照)。Codex CLI/Antigravity/Cursorは既出動向の延長のみで乗り換え動機なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、CachyOSは9月分未発表。
- D (業務SaaS/AI): 動きなし。freee顧問先管理AIエージェントの9/15開始が明日に迫っている以外、マネーフォワード/弥生含め9月固有の乗り換え動機となる新着は確認できず。
- E (ハード/インフラ): 動きなし。ROCm/Tailscale/1Password CLIとも実質的な変化なし(Tailscaleは内容確認止まりでバージョン確認は宿題のまま)。
