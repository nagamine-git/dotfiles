# tool-radar 2026-08-26 (期間 2026-08-25 → 2026-08-26)

検出 10件 / 取込価値 1件

## 今日の結論

Claude Code v2.1.246 で `/permissions` に Auto mode タブが追加され、8/15付で「要確認」のまま放置していた `defaultMode: "auto"` 設定の中身 (classifier rules) を初めて画面上で確認・編集できるようになった。同バージョンでは MCP ツール引数が実際の型ではなく JSON 文字列として送信されるバグも修正されており、freee-mcp / Slack MCP 経由のツール呼び出しに地味な実害があった可能性がある。他は競合CLI (Meta Muse Code) の存在確認や Cursor の買収報道など情報レベルの動きに留まり、新規導入を検討すべきものはない。

## 推奨アクション (最大3)

1. **Claude Code v2.1.246 — Auto mode タブ** ★★ [確認済] — `/permissions` に Auto mode タブが追加され、auto mode の classifier rules を表示・編集できるようになった。既存: `dot_claude/private_settings.json.tmpl` で `"defaultMode": "auto"` を使用中だが、8/15の radar で「どう判定されているか要確認」のまま放置していた項目。同バージョンでは MCP ツール引数の型バグ (JSON文字列化) も修正されており、freee-mcp・Slack MCP 呼び出し品質に間接的にプラス。移行コスト: 低 — 通常のアップデートを受けて `/permissions` の Auto mode タブを一度眺めるだけ。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Cursor** ★ [推定] [再掲:変化あり] — SpaceXがCursorを$60Bで買収しxAI/Grokと統合するとの報道 (6/16付、Q3完了予定) を確認。買収完了までCursor CLIは独立運用継続とされるが、技術方向性が変わりうる大きな状況変化のため一段格上げして記録。現時点では乗り換え動機なし。
- **Meta Muse Code / Muse Spark 1.2** ⌀ [確認済] — Metaが8/5に投入した新規ターミナル型コーディングエージェント。ベンチマークはClaude Code未満・Codex CLI以上の位置づけで、価格訴求が主眼。乗り換え動機なし、情報として記録のみ。
- freee Agent Hub / freee顧問先管理AIエージェント (8/28正式開始まで2日) ⌀ — 内容変化なし、引き続き税理士事務所向けのため非該当。
- Tailscale TS-2026-009/010/011 ⌀ — 修正版 (v1.98.9以降 / v1.102.3) 判明済みだが実機の `tailscale version` 確認は依然未実施。
- ROCm.ai / amd-skills、CachyOS 8月リリース、Hyprland 0.56.2 ⌀ — いずれも既出内容から変化なし。Hyprland 0.56.3/0.57は未リリース。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。nvim (telescope.nvim/blink.cmp/conform.nvim/nvim-treesitter)・sheldon・tmux plugin群・mise・lazygit・jujutsuとも今日付の新着なし。
- B (AIエージェント): Claude Code v2.1.246 (上記、実利アクション) が唯一の新規動意。Cursor買収報道・Meta Muse Code登場は情報レベル。Codex CLI/Aider/Gemini CLIとも通常運用の範囲内で新規性なし。
- C (WM/OS): Hyprland 0.56.2が最新のまま新版なし。CachyOS 8月リリース・niri/river/swayに動きなし。
- D (業務SaaS/AI): freee Agent Hub正式開始 (8/28) が2日後に迫るのみ、内容変化なし。会計・請求・スケジュール・ナレッジ系に新規動意なし。
- E (ハード/インフラ): Tailscale監視継続 (実機確認待ち)。ROCm.aiは既出範囲内。Uptime Kuma 2.5.0 (npm 14日クールダウン導入) はリリースされたが未導入のため非該当。Restic/Kopia/Beszelに今日付の新規リリースなし。
