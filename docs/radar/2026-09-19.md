# tool-radar 2026-09-19 (期間 2026-09-18 → 2026-09-19)

検出 12件 / 取込価値 0件

## 今日の結論

取り込むべき新規なし。現状維持で十分。唯一の新着は Claude Code v2.1.278(9/19)で、Claude API・Enterprise・Bedrock/Vertex/Foundry/gateway利用者向けにauto modeの分類器をサーバー側実行がデフォルト化され分類器分のコストが無課金になる変更が入ったが、当環境はMax契約+直接API(dot_claude/private_settings.json.tmplで`defaultMode: auto`のみ、Bedrock/Vertex/gateway系設定なし)のため対象条件に該当するか不明。他にAGENTS.md未設置時のフォールバック読み込み対応、`claude -p`/Agent SDKがinternalエラー後に無応答でハングする不具合の修正が入ったが、いずれも自動適用または既存運用(CLAUDE.md使用)に非直撃。A(開発環境)はlazygit/jujutsu/zellij/blink.cmpとも9/18-19固有の新着なし、C(WM/OS)はHyprland 0.57が進捗20%のまま・CachyOS9月版もROCmリポジトリ反映も未だ動意なし、D(業務SaaS)はfreee-mcp・freee本体とも新規発表なし、E(ハード/インフラ)はTailscale・Ghosttyとも無風。Cursor/Codex/Gemini CLI/Antigravityのサンドボックスエスケープ報道が3日連続で検索結果に再浮上しているが、依然7月の既出インシデントの再検出のみで新規性はない。

## 推奨アクション (最大3)

(該当なし — 取り込むべきアクションなし)

## 監視継続

- **Claude Code v2.1.278 (9/19) — auto modeサーバー側分類器デフォルト化** ⌀ [確認済] — 対象は「Claude API・Enterprise、およびBedrock/Vertex/Foundry/gateway」利用者向けと明記されており、当環境(claude.ai Max契約+直接API、Bedrock等未使用)が該当するか公式文言からは断定できない。`/status`に`Auto mode server`行が追加されたため、次回起動時に実機確認すれば当環境への適用有無が判明する。他にAGENTS.md未設置プロジェクトへのフォールバック読み込み、`claude -p`/Agent SDKのinternalエラー後ハングバグ修正が同時収録。
- **freee-mcp v0.35.2 最新版確認** ⌀ [確認済] — 9/16判定から変化なし。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然rocm-core 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了、8/20更新のまま)変化なし。
- **Tailscale最新版** ⌀ [確認済] — 公式changelog最終更新は9/12のまま新着なし、デスクトップ実機バージョン確認は宿題継続。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/19時点で未発表、直近は8月リリースのまま。
- **Ghostty 1.4.0** ⌀ [確認済] — 依然未リリース、安定版1.3.1のまま。
- **Cursor/Codex/Gemini CLI/Antigravity サンドボックスエスケープ報道 (3日連続再検出)** ⌀ [推定] — 7月の既出報道が検索結果に繰り返し再浮上しているだけで新規性なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazygit/jujutsu/zellij/blink.cmp/telescope.nvim/sheldon/tmuxプラグイン群とも9/18-19固有の新着なし。
- B (AIエージェント): Claude Code v2.1.278のみ(内容は上記)。Antigravity CLI v1.2.4(9/16、スキルリロード等)は通常アップデートの範囲で乗り換え動機なし。Cursor/Codex CLIとも9/18-19固有の新着なし。
- C (WM/OS): 動きなし。Hyprland進捗変化なし、CachyOS9月版未発表、ROCmリポジトリも反映待ち継続。Arch Linux本家は2026.09.01 ISO(9/2既知)が引き続き最新で新規性なし。
- D (業務SaaS/AI): 動きなし。freee-mcp v0.35.2から変化なし、freee本体・マネーフォワード等も9月固有の新規発表は確認できず。
- E (ハード/インフラ): 動きなし。Tailscale/Ghosttyとも実質変化なし、自宅サーバー監視・バックアップ系(Beszel/Uptime Kuma/Duplicati/restic)も9/18-19固有の新着確認できず。
