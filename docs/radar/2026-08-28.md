# tool-radar 2026-08-28 (期間 2026-08-27 → 2026-08-28)

検出 9件 / 取込価値 3件

## 今日の結論

Claude Code v2.1.251 に、この環境の実際の穴を埋める変更が3件同時に来た。(1) plugin marketplace のコマンドがプラグインディレクトリ外を指せる脆弱性の修正は、`freee-mcp-marketplace` という外部リポジトリを含む5プラグインを有効化している当環境に直撃するセキュリティ修正。(2) `/cost` への prompt-cache 内訳表示追加は、8/27付で導入した agent frontmatter の `experimental.cacheTtl` が実際に効いているかを今日から定量確認できるようにする。(3) `PreModelSwitch`/`PostModelSwitch` フックは、`dot_claude/CLAUDE.md` の「モデル配車則」(Fable週次枠80%超で自動降格) を口頭ルールのままにせず仕組みで強制できる空いてる穴。他は freee Agent Hub の予定通りの正式公開 (非該当) や ROCm 10.0 のCachyOSリポジトリ未反映確認など、既出事項の状況更新に留まる。

## 推奨アクション (最大3)

1. **Claude Code v2.1.251 — plugin marketplace ディレクトリ脱出 + symlinkパーミッション回避の修正** ★★★ [確認済] — marketplaceエントリで宣言されたplugin commandsがプラグインディレクトリ外を指せる不具合、Read/Write/Editがパーミッションチェック後にディレクトリ内でsymlinkをすり替えられて追従してしまう不具合、Grep/Globがsymlink経由の検索パスでは `Read(...)` のdenyルールを回避してしまう不具合を修正。既存: `dot_claude/private_settings.json.tmpl` の `enabledPlugins` で `freee-mcp@freee-mcp-marketplace` (外部リポジトリ `github:freee/freee-mcp`) を含む5プラグインを有効化しており、まさにこの脆弱性クラスの実害範囲。移行コスト: 低 — 通常の自動更新で反映されるのを確認するだけ。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.251 — `/cost` への prompt-cache 内訳追加** ★★ [確認済] — セッション単位のprompt-cacheヒット率・ミス数・再キャッシュトークン数・warm/cold内訳が `/cost` に表示されるようになった。既存: 8/27付radarで採用を推奨した agent frontmatter の `experimental.cacheTtl` (`dot_claude/agents/reviewer.md`/`implementer.md`/`researcher.md`) が実際にキャッシュヒット率を改善しているか、これまで確認手段がなかった空いてる穴が埋まる。移行コスト: 低 — 次回セッションで `/cost` を叩くだけ。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.251 — `PreModelSwitch`/`PostModelSwitch` フック追加** ★★ [確認済] — モデル切替をブロック・確認・注釈できるフックイベントが新設。既存: `dot_claude/CLAUDE.md` の「モデル配車則」に "Fableセッション中に週次枠>80%ならOpus (fast) に自主降格" という自己申告ベースのルールがあり、これまでは仕組みでの強制手段がなかった。`PreModelSwitch` で週次枠消費率をチェックしFable切替をブロック/確認する、という実装が考えられる。移行コスト: 中 — フックスクリプトを新規に書く必要がある (週次枠消費率の取得元を `claude-usage` 系スクリプトと連携させる設計が要る)。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **ROCm 10.0 の CachyOS リポジトリ反映状況** ⌀ [確認済] — `packages.cachyos.org` を確認したところ `rocm-core`/`rocm-hip-sdk` 等は依然 7.2.4 のまま。8/27付radarの「CachyOSリポジトリへの反映を待ってから追従が無難」という判断を維持。
- **freee-mcp と MCP仕様2026-07-28 の対応状況** ⌀ [確認済] [再掲:変化あり] — 8/15から「対応状況不明」のまま監視継続していた件、CHANGELOG.md を直接確認したところ v0.32.0 (8月中旬) で `Mcp-Protocol-Version`/`Mcp-Method`/`Mcp-Name` ヘッダーへのCORS対応が入っており、フルのstateless実装ではなく **準備段階** であることが判明。v0.28.0で `/health` エンドポイントが廃止され `/livez`/`/readyz` に分離されている点も、もし自作の死活監視スクリプトが `/health` を叩いていれば要修正 (dotfiles内には該当スクリプト無し、実機のみ要確認)。
- **Claude Code v2.1.251 — Opus 5 の effort パラメータ不具合修正** ⌀ [確認済] — 「thinking無効時にeffortパラメータ未対応」エラーでOpus 5リクエストが失敗する不具合を修正。`dot_claude/private_settings.json.tmpl` は `effortLevel: high` かつメインループが Opus 5 既定のため、地味に実害があった可能性がある修正。通常更新で追従でよい。
- **Tailscale** ⌀ [確認済] — PAM (Privileged Access Management) がベータ追加されたのみで、TS-2026-009/010/011 に関する状況変化なし。実機の `tailscale version` 確認は依然未実施のまま。
- **freee Agent Hub / freee顧問先管理AIエージェント** ⌀ — 本日8/28、予告通り正式提供開始。引き続き税理士事務所向けのため非該当。
- **Codex CLI v0.150.1 (Agent Plugins対応・MCP 2026-07-28対応)** ⌀ [確認済] — Amazon/Anysphere/Microsoft/OpenAI/Vercelの Agent Plugins 1.0 標準に競合CLI側が追従。Claude Codeは8/17時点で未対応表明のまま。乗り換え動機なし、エコシステム動向として記録のみ。
- **Cursor Origin 正式ロールアウト開始** ⌀ — 8/18付radarで「チーム/クラウド向けで一人法人ローカル運用に非該当」と判定済みの機能が本日ロールアウト開始。判断に変化なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。telescope.nvim/blink.cmp/neo-tree/lazygit/jujutsuとも今日付の新着・破壊的変更なし。
- B (AIエージェント): Claude Code v2.1.251 (上記、実利アクション3件) が中心。Codex CLI/Cursorは既出方向性の延長のみ。
- C (WM/OS): Hyprland 0.57は依然milestone進行中で未リリース。CachyOS 8月リリースは既出内容の反復のみで新規性なし。niri/river/swayに動きなし。
- D (業務SaaS/AI): freee Agent Hub正式提供開始 (本日、予告通り) のみ、内容変化なし。
- E (ハード/インフラ): ROCm10.0のCachyOS未反映確認・freee-mcpのMCP仕様対応状況判明 (上記) が実質的な動き。Tailscaleは実機確認待ちのまま変化なし。
