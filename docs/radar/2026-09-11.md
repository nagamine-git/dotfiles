# tool-radar 2026-09-11 (期間 2026-09-10 → 2026-09-11)

検出 13件 / 取込価値 4件

## 今日の結論

Claude Code v2.1.269 (9/11) に、現環境の日常運用へ直接効く変更が複数まとまって来た。特に「Bash `tee` で書き込んだファイルに `Edit()` のdeny規則・write-pathチェックが適用されない」バグ修正は、8/28・9/2・9/9・9/10 と続いてきた「権限deny規則のバイパス系」修正の5件目であり、`private_settings.json.tmpl` の `permissions.deny` (1Password・force push・publish系) が回避されうる経路をまた一つ塞いだ。さらに「分かち書きしない言語 (日本語・中国語・タイ語等) でプロンプト候補が消える」バグ修正は、`"language": "japanese"` 設定で日常的に日本語入力している当環境に直接効く地味だが実利のある修正。それ以外のA(開発環境)/C(WM・OS)/D(業務SaaS)/E(ハード・インフラ)は無風。

## 推奨アクション (最大3)

1. **Claude Code v2.1.269 — Bash `tee` 経由の `Edit()` deny規則・write-pathチェック回避バグ修正** ★★★ [確認済] — Bashの `tee` コマンドが書き込むファイルに `Edit()` のdeny規則・write-pathチェックが適用されておらず、`Bash(tee:*)` のallowルールが作業ディレクトリ外の書き込み先までカバーしてしまっていた不具合を修正。既存: `dot_claude/private_settings.json.tmpl` の `permissions.deny` (`op item*`/`op read*`等1Password保護、`git push --force*`、`npm/yarn/pnpm/cargo publish*`等) が `tee` 経由で回避されうる経路だった。8/28(symlinkパーミッション)・9/2(symlink経由脱出)・9/9(バックスラッシュ経由)・9/10(symlink綴り/env・eval経由)に続く同系統5件目の修正で、この設計領域が繰り返し穴を抱えていたことを示す。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.269 — 日本語等分かち書きなし言語でのプロンプト候補ドロップ修正** ★★★ [確認済] — 日本語・中国語・タイ語等、単語間にスペースを置かない言語のテキストでプロンプト候補(prompt suggestions)が消えていた不具合を修正。あわせて日本語・中国語・韓国語のプロンプト候補フィルタリングを改善(混在スクリプト・単語のみの候補は残し、メタ・評価的なテキストは英語同様に除外)。既存: `dot_claude/private_settings.json.tmpl` の `"language": "japanese"` 設定で日常的に日本語入力している当環境の日次UXに直接効く実利ある修正。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.269 — プラグインアーカイブの他ローカルユーザー読み取り可能・world-writableビット残存バグ修正** ★★ [確認済] — セッション用に展開されたプラグインアーカイブが他のローカルユーザーから読み取り可能だった不具合、アーカイブ内のworld-writableビットが展開後のファイルに残ってしまう不具合、再展開時に古いファイルが残存してしまう不具合を修正。既存: `dot_claude/private_settings.json.tmpl` の `enabledPlugins`/`extraKnownMarketplaces` で外部リポジトリ製 `freee-mcp@freee-mcp-marketplace` を有効化中で、マルチユーザー環境でのプラグイン展開ファイル露出リスクを直接減らす。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.269 — attribution reminderがCLAUDE.md/memoryルールを上書きしてしまうバグ修正** ★★ [確認済] — コミット・PRの attribution に関する CLAUDE.md やメモリのルールを、システム側の attribution reminder が上書きしてしまう不具合を修正 (managed settings 由来のルールは引き続き優先)。既存: `dot_claude/CLAUDE.md` にattribution関連の明示ルールは無いが、リポジトリ運用でCLAUDE.mdのルールが実際に尊重される保証が強化された形。今回は推奨アクション上限のため次点扱い、実害はなく監視のみ。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然 `rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし、安定版は引き続き0.56.2。
- **Tailscale最新版** ⌀ [推定] — TS-2026-011(host-scoped IPv4/4via6)修正・Go 1.26.6更新は9/9-10時点と同一内容、公式サイト直接確認は今回も不確実で実機`tailscale version`確認は宿題のまま。
- **freee-mcp 最新版確認** ⌀ [不明] — 検索結果がv0.15.0〜v0.24.0という明らかに古い/矛盾したバージョン番号を返し、9/10時点で確認済みのv0.35.1との整合が取れず。検索エンジン側の情報鮮度低下とみられ、確度を持って新版なしとは言い切れない。次回、公式GitHub Releasesページの直接確認を優先する。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/11時点で未発表、直近は8月リリースのまま。
- **freee顧問先管理 | AIエージェント 9/15正式提供開始** ⌀ [確認済] — 予定日が4日後に接近、税理士事務所向けのため当環境(一人法人・自社利用)には引き続き非該当。
- **Antigravity CLI Gemini 3.8 Flash / Codex CLI / Cursor 動向** ⌀ [推定] — 競合CLIのモデル更新・エコシステム追従のみ、乗り換え動機なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/telescope.nvim/lazygit/jujutsu/sheldon/tmuxプラグイン群とも9/11固有の新着確認できず。
- B (AIエージェント): Claude Code v2.1.269に実利ある変更4系統あり(上記参照)。Antigravity CLI(Gemini 3.8 Flash)/Codex CLI/Cursorは競合エコシステムの延長のみで乗り換え動機なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、CachyOSは9月分未発表。
- D (業務SaaS/AI): 動きなし。freee顧問先管理AIエージェントの9/15開始が近づいている以外、マネーフォワード/弥生含め9月固有の新着・乗り換え動機となる発表は確認できず。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし(Tailscaleは直接確認不可のため推定、freee-mcpは検索結果の信頼性低下により確度不明)。
