# tool-radar 2026-09-09 (期間 2026-09-08 → 2026-09-09)

検出 13件 / 取込価値 3件

## 今日の結論

Claude Code v2.1.267 (9/9) に、現環境の構成へ直接効く変更が3件まとまって来た。特に「マーケットプレイスのエントリパスにバックスラッシュを含む場合、取得済みマーケットプレイスのcontainmentチェックをmacOS/Linuxで回避できる」脆弱性の修正は、外部リポジトリ製プラグイン(`freee-mcp@freee-mcp-marketplace`)を有効化している当環境の実害範囲に直撃する — 8/28・9/2に続く同系統(プラグイン/マーケットプレイスのディレクトリ脱出)の3件目の修正であり、この設計領域自体が繰り返し穴を抱えていることを示唆する。あわせて `maxEffortLevel` 設定の追加、`Workflow` の `agent()` 呼び出しがauto modeで誤ブロックされていたバグの修正も実利がある。それ以外のA(開発環境)/C(WM・OS)/D(業務SaaS)/E(ハード・インフラ)は無風。

## 推奨アクション (最大3)

1. **Claude Code v2.1.267 — マーケットプレイスパスのバックスラッシュ経由containment回避バグ修正** ★★★ [確認済] — 取得済みマーケットプレイスのエントリパスにバックスラッシュを含めることで、macOS/Linuxでcontainmentチェック(自身のディレクトリ外へのアクセス制限)を回避できてしまう脆弱性を修正。既存: `dot_claude/private_settings.json.tmpl` で外部リポジトリ (`github:freee/freee-mcp`) の `freee-mcp-marketplace` を有効化しており、8/28 (symlinkパーミッション回避)・9/2 (symlink経由ディレクトリ脱出) に続き同系統の脆弱性が3件目。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.267 — `maxEffortLevel` 設定追加** ★★ [確認済] — トップレベルまたは`modelSettings`配下でモデルごとに実効effortレベルの上限を設定可能に(Bedrock/Vertex/Foundry含む全プロバイダに適用、ユーザーはそれ以下を選択可)。既存: `dot_claude/private_settings.json.tmpl` の `"effortLevel": "high"` と `CLAUDE.md` の「モデル配車則」(Fable週次枠>80%でOpus fastへ自主降格等)を、設定で仕組み化できる空いてる穴。移行コスト: 低〜中 — 現行は運用ルール(CLAUDE.md記述)に依存した自主降格のため、`maxEffortLevel`で機械的に強制するかは要検討。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.267 — Workflow `agent()` の大型出力スキーマがauto modeで誤ブロックされるバグ修正** ★★ [確認済] — 出力スキーマが大きい`agent()`呼び出しが、安全性分類器によるチェックではなくauto modeで一律拒否されていた問題を修正。既存: `defaultMode: "auto"` を全面採用しており、`Workflow`ツール(review-changes等の多段パイプライン)で構造化出力スキーマを使う際に誤ブロックを受けていた可能性がある空いてる穴。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.266 (9/8) — `CLAUDE_CODE_USE_GATEWAY` 環境変数の2.1.265退行修正** ⌀ [確認済] — LLMゲートウェイ/プロキシ構成で強制的にCloud gatewayサインインを要求してしまう退行を修正。既存: 直接API+Slack MCP運用でゲートウェイ/プロキシ未使用のため非該当。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — `packages.cachyos.org`で依然`rocm-core` 7.2.4のまま(6/2ビルド)、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — GitHub上で進捗20%(5件中1件完了)・8/20更新のまま変化なし、安定版は引き続き0.56.2。
- **Tailscale最新版** ⌀ [推定] — 公式`tailscale.com`は今回もプロキシでブロックされ直接確認不可。検索結果ではTS-2026-011(host-scoped IPv4/4via6)修正やGo 1.26.6更新への言及があったが、掲載日時・バージョン番号とも特定できず新規リリースの確証なし。実機`tailscale version`確認は宿題のまま。
- **freee-mcp** ⌀ [確認済] — v0.35.1(9/8)が最新のまま、新規リリースなし。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/9時点で未発表。
- **マネーフォワード/弥生/freee AI機能動向** ⌀ [確認済] — freeeが6月開始済みの「freee AIアシスタント」等の一般比較記事のみで、9月固有の新着・乗り換え動機となる具体的発表は確認できず。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/snacks.nvim/telescope.nvim/lazygit/jujutsu/sheldon/tmuxプラグイン群とも9月固有の新着なし。
- B (AIエージェント): Claude Code v2.1.266/267に実利ある変更あり(上記参照)。Cursor/Codex CLI/Gemini CLI(Antigravity統合済み)/Aiderは既出動向の延長のみで9/9固有の新着なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし。niri/river/swayもSway 9/6更新以外は9月固有の新着なし(Hyprland非使用のため非該当)。CachyOSは9月分未発表。
- D (業務SaaS/AI): 動きなし。freee/マネーフォワード/弥生いずれも9月固有の乗り換え動機となる新着確認できず。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし(Tailscaleは直接確認不可のため推定)。
