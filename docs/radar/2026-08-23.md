# tool-radar 2026-08-23 (期間 2026-08-22 → 2026-08-23)

検出 4件 / 取込価値 0件

## 今日の結論

前回実行から1日のため窓は極めて狭く、検出できたのは全て既導入ツールの通常アップデートか既出の未確定事項の後追い確認のみ。**取り込むべき新規なし。現状維持で十分。**

## 推奨アクション (最大3)

無し。(Tailscale の件は下記「監視継続」参照 — 状況が変わり緊急性が下がった。)

## 監視継続

- **Tailscale バージョン確認事項の状況更新** ⌀ [確認済] — 8/21付で推奨した TS-2026-009/010 対応版 (v1.98.9以降) について、Tailscale の現行stableは既に v1.102.3 (8/19リリース) まで進んでいることを確認した。CachyOSはローリングリリースで pkglist.txt 上も tailscale はバージョン固定なしのため、通常の `pacman -Syu` 運用をしていれば当該脆弱性は既に解消されている可能性が高い。実機で `tailscale version` が v1.98.9 以上（できれば最新の v1.102系）であることの確認だけ済ませれば、この監視事項はクローズしてよい。
- Claude Code v2.1.241 (8/23) ⌀ [確認済] — voice mode が接続拒否時に "listening…" のまま固まる不具合修正、mTLSクライアント証明書ローテーション時に再起動不要で自動リロードする修正、Remote Control セッションで途中から接続した閲覧者にも保留中の権限プロンプトが正しく表示されるようになる修正。当環境はSlack MCP等でmTLS非使用、voice modeも常用構成に見当たらないため直接の実利は薄いが、Remote Control (このセッション自体が該当) の表示修正は地味に有用。通常の自動更新で追従でよい。
- Codex CLI v0.148.0 (8/18確認) ⌀ [確認済] — `/export`でのMarkdown書き出し、`codex exec fork`によるセッション分岐、レジューム画面でのアーカイブ/復元、Bedrock Runtime組み込みプロバイダ化、非同期フック+MCPツール呼び出し、サンドボックスの拒否/読み取り不能パスでのfail-closed化。競合CLIの通常アップデートで乗り換え動機なし。
- CachyOS / Hyprland / lazygit / jujutsu / freee Agent Hub (8/28提供開始予定) ⌀ — いずれも8/22以前に既出の内容から変化なし。今日付の新規発表は無し。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。nvim(lazy.nvim/blink.cmp/telescope/neo-tree/conform)・sheldon・tmux plugin群・mise・lazygit(v0.64.1が最新のまま)・jujutsu(v0.45.0が最新のまま)とも今日付の新着なし。
- B (AIエージェント): Claude Code v2.1.241(通常更新)、Codex CLI v0.148.0(既出の続報)が検出のみ。Cursor/Aider/Zed/Antigravityとも今日付の具体的発表なし。MCP新ロードマップ(8/22既出)から進展なし、freee-mcpの2026-07-28仕様対応状況も引き続き不明のまま。
- C (WM/OS): Hyprland 0.56.2が最新のまま(0.57はmilestone進行中で未リリース)。CachyOS 8月リリース・Arch Linux 8月ISO(AUR乗っ取り対策含む)とも既出情報の範囲内。niri/river/swayとも地殻変動なし。
- D (業務SaaS/AI): freee Agent Hub正式提供(8/28予定)は既出のまま日付接近のみ。会計・請求・スケジュール・ナレッジ系に新規動意なし。
- E (ハード/インフラ): Tailscaleの状況更新(上記、監視事項のクローズ見込み)。ROCm 7.14が最新のまま(ROCm.AIの正式統合は依然未確定)。自宅サーバ監視・バックアップ系(Uptime Kuma 2.5.0は8/1リリースで既に一般化、Restic/Kopiaは7月以前のバージョンのまま)に新規リリースなし。
