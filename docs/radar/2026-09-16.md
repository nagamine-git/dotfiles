# tool-radar 2026-09-16 (期間 2026-09-15 → 2026-09-16)

検出 9件 / 取込価値 0件

## 今日の結論

取り込むべき新規なし。現状維持で十分。Claude Code は v2.1.273(9/15)から新規リリースなく、A(開発環境)/C(WM・OS)/E(ハード・インフラ)は軒並み無風。D(業務SaaS)では freee-mcp が v0.35.2 へパッチ更新されたが工数管理スキルの微修正とスキーマ同期のみで実質的な機能変更なし。唯一の話題は競合 Cursor が「Projects」(クラウド常駐のコーディネーターエージェントが多数のサブエージェントに作業委任し、PC を閉じても継続する機能)を正式ロールアウトした点だが、これは当環境で既に使っている Claude Code の Workflow(サブエージェント委任)と同方向の機能であり、乗り換え動機にはならない。

## 推奨アクション (最大3)

(該当なし — 取り込むべきアクションなし)

## 監視継続

- **Cursor「Projects」正式ロールアウト** ★ [確認済] — コーディネーターエージェントが機能追加・移行作業を多数のサブエージェントに委任し、クラウド上で継続実行する新機能(cursor.com/blog/projects)。Claude Code の Workflow/サブエージェント運用と同方向の後追い機能であり、乗り換え動機なし。情報として記録のみ。
- **freee-mcp v0.35.2** ⌀ [確認済] — 工数管理スキルへのリンク追加、別表ファイルの `index.md` 統合(200ファイル上限対応)、OpenAPIスキーマ同期のみのパッチ更新。9/8時点で既知の `launch`(freee開業)/`employee_evaluation` ドメイン分割(v0.35.0)から実質変化なし、一人法人には引き続き非該当。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然 `rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし。
- **Tailscale最新版** ⌀ [確認済] — Android版 v1.102.4(9/11)が最新、内容(TS-2026-011修正・Go 1.26.6等)は9/9以降と同一。デスクトップ版の実機 `tailscale version` 確認は引き続き宿題。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/16時点で未発表、直近は8月リリースのまま。
- **supermaven-nvim** ⌀ [確認済] — 9/13判定(本体サービスsunset済み・Neovim向け無料推論のみ「当面継続」)から状況変化なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazygit/jujutsu/zellij/blink.cmp/telescope.nvim/snacks.nvim/sheldon/tmuxプラグイン群とも9/16固有の新着確認できず。
- B (AIエージェント): Claude Code は9/15のv2.1.273から新規リリースなし。Codex CLI/Gemini CLI/Antigravityも9/14時点の既出動向から進展なし。Cursor「Projects」正式ロールアウトのみ監視継続へ記録。MCPエコシステムは7月仕様・8月ロードマップ以降の新規動向なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、CachyOSは9月分未発表。
- D (業務SaaS/AI): freee-mcp v0.35.2はパッチ更新のみで非該当。一人社長向けAI SaaSトレンド記事はあるが具体的な乗り換え候補なし。
- E (ハード/インフラ): 動きなし。ROCm/Tailscale/freee-mcpとも実質的な変化なし。自己ホスト監視系(Uptime Kuma/Grafana/Prometheus)にも9月固有の新着なし。
