# tool-radar 2026-09-15 (期間 2026-09-14 → 2026-09-15)

検出 13件 / 取込価値 3件

## 今日の結論

Claude Code が v2.1.271 → v2.1.272 → v2.1.273(9/15)へ更新され、v2.1.273 は久々の大型リリースだった。特に「コンテキストメーター/auto-compact が advisor-tool ターンを実際の約2倍でカウントし、本来より早く auto-compact が発動していた」バグ修正は、effort high・max レビュー艦隊運用でコンテキスト消費が多い当環境のコスト管理に直結する実利があり、今回のトップ推奨とした。一方で 2026-09-10(v2.1.268)で入った「eval/env -C 経由 Bash ラインの Read/Edit deny 規則チェック」が誤検知対策で部分的にリバートされており、8/28 以降続いてきた permissions.deny 実効性強化の流れが一部後退した点は把握しておくべき変化として記録する。それ以外の A(開発環境)/C(WM・OS)/E(ハード・インフラ)は無風。D(業務 SaaS)では「freee顧問先管理 | AIエージェント」が予定通り本日正式提供開始したが、税理士事務所向けのため引き続き非該当で監視終了とする。

## 推奨アクション (最大3)

1. **Claude Code v2.1.273 — コンテキストメーター/auto-compact の advisor-tool ターン2倍カウントバグ修正** ★★★ [確認済] — advisor-tool のターンが実際のコンテキストサイズの約2倍としてカウントされ、auto-compact が実質的なウィンドウの約半分の時点で発動していた不具合を修正。既存: `dot_claude/CLAUDE.md` の「max レビュー艦隊」運用や `dot_claude/private_settings.json.tmpl` の `effortLevel: "high"` など、長時間・高コンテキストのセッションを日常的に回す当環境では、不要に早い auto-compact はコンテキスト圧縮の頻度増(=API コスト増・文脈の断絶)に直結する空いてる穴だった。移行コスト: 低 — 自動適用のバグ修正、通常更新で追従するだけでよい。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.273 — 2.1.268 の Read/Edit deny 規則(eval/env -C 経由 Bash ライン)チェックを部分リバート** ★★ [確認済] — 2026-09-10(v2.1.268)で追加された「`eval`/`env -C` 経由の Bash ラインにも Read/Edit の deny 規則を適用する」修正が、`time -p make build` のような正当なコマンドまで誤って deny してしまう副作用のため、今回リバートされた。既存: `dot_claude/private_settings.json.tmpl` の `permissions.deny` は `git push --force`・`op item`・`gh repo delete` 等の Bash パターンマッチに依存しており、8/28・9/2・9/9・9/10・9/11 と続いてきた「permissions.deny バイパス系の実効性強化」の流れが一部後退した形。独自実装の `dot_claude/hooks/executable_injection-guard.sh` フックは影響を受けないが、deny 規則単体で頼れる範囲が狭まった点は把握しておくべき。移行コスト: 低 — 対応不要、動作追従のみ。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.273 — Remote Control セッションのフォーク機能追加** ★★ [確認済] — `claude --remote-control`/`/remote-control` で開始したセッション、または Claude アプリ(スマホ/Web)からのセッションを、手元マシン上のバックグラウンドセッションとしてフォークできるようになった。既存: `dot_claude/private_settings.json.tmpl` で `"remoteControlAtStartup": true` を採用中で、外出先からスマホ/Web でセッションの状況を見て、そこから作業を実マシンのバックグラウンドへ切り出せる、今までなかった動線が空いてる穴として埋まる。移行コスト: 低 — 追加設定不要、既存の remoteControlAtStartup 構成でそのまま利用可能。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **freee顧問先管理 | AIエージェント 正式提供開始(本日 9/15)** ⌀ [確認済] — 予定通り本日有料提供開始(記帳・ルール設定・月次確認・決算申告確認の4種エージェント)。税理士事務所向け機能のため一人法人・自社利用の当環境には引き続き非該当、9/5から続けてきたこの監視事項はここで終了。
- **Claude Code v2.1.273 — scheduled_tasks.json が別フォルダ(worktree等)へコピーされた際に誤ったセッションで実行されるバグ修正** ⌀ [確認済] — 本 radar 自体がスケジュール実行されるセッションのため関連はするが、worktree コピー等の特殊ケースの修正で通常運用への実害は小さい、自動適用で追従。
- **freee-mcp v0.35.1 最新版確認** ⌀ [確認済] — GitHub Releases直接確認、9/8以降の新規リリースなし。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然 `rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし。
- **Tailscale最新版** ⌀ [推定] — 内容(TS-2026-011修正・Go 1.26.6・Kubernetes Operator改善等)は9/9以降と同一、実機`tailscale version`確認は依然宿題のまま。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/15時点で未発表、直近は8月リリースのまま。
- **Cursor/Codex CLI/Gemini CLI/Antigravity サンドボックスエスケープ報道** ⌀ [推定] — 検索結果に再浮上したが7月時点の既出報道で新規性なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazygit/jujutsu/zellij/blink.cmp/telescope.nvim/sheldon/tmuxプラグイン群とも9/15固有の新着確認できず。
- B (AIエージェント): Claude Code v2.1.272/273に多数の変更。実利ある3系統を上記推奨アクションに、scheduled_tasks修正は監視継続へ。Cursor/Codex CLI/Gemini CLI/Antigravityは既出動向の延長のみで乗り換え動機なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、CachyOSは9月分未発表、Arch Linux 2026.09.01 ISOも既出内容のまま。
- D (業務SaaS/AI): freee顧問先管理AIエージェントが本日正式提供開始(予定通り、非該当のため監視終了)。それ以外は動きなし。
- E (ハード/インフラ): 動きなし。ROCm/Tailscale/freee-mcpとも実質的な変化なし。
