# tool-radar 2026-08-21 (期間 2026-08-20 → 2026-08-21)

検出 5件 / 取込価値 0件

## 今日の結論

新規ツールの導入候補なし。ただし既出の未解決監視事項だった Tailscale SSH root権限バイパス (TS-2026-009/010) の修正バージョンが確認できた。実機の `tailscale version` 確認を推奨アクションとして格上げする。**取り込むべき新規なし。現状維持で十分。**

## 推奨アクション (最大3)

1. **Tailscale アップデート確認 (TS-2026-009/010)** ★★ [確認済] — 修正版は v1.98.9 以降。当環境は taildrop / waybar tailscale-status・exit-node picker / bin/server.sh など tailscale 常用構成のため実害範囲が広い。既存: pacman管理のtailscale (pkglist.txtに記載、バージョン固定なし)。移行コスト: 低 — `tailscale version` で現在値を確認し、v1.98.9未満なら `sudo pacman -Syu tailscale` のみで完結。CachyOSはローリングリリースのため既に取り込み済みの可能性が高いが未確認のまま8/15から6日経過しており要実機確認。URL: https://tailscale.com/changelog

## 監視継続

- Claude Code v2.1.238/v2.1.239 (8/20-21) ⌀ [確認済] — 長時間インタラクティブセッションでのメモリ無制限増加を修正 (v2.1.238)、リモートMCPサーバが一時的な5xx後に「失敗」状態のまま復帰しない不具合を修正 (v2.1.239)。当環境はSlack MCP(リモートhttp)・freee-mcp/playwright/chrome-devtools-mcp(stdio)を常用するため実利あり。新機能ではなく安定性修正のため推奨アクション化はせず、通常の自動更新に任せてよい。
- MCPエコシステムのセキュリティ懸念 (8/13-14 Seoul Dev Summit, OX Security報告) ⌀ [確認済] — 公開MCPサーバの9割超がOAuth未実装という報告。当環境のfreee-mcpはOAuth2.0+PKCE採用済み、Slack MCPも公式OAuth経由、かつdot_claude/hooks/injection-guard.shで防御済みのため直接のアクション不要。一般的な業界動向として記録のみ。
- freee-mcp 継続更新 (freeeサイン対応・IT管理対応、既出) ⌀ — 今回新着なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazy.nvim/blink.cmp/neo-tree/lazygit/jujutsuとも今日付の破壊的変更や乗り換え動機になる新着なし。
- B (AIエージェント): Claude Code v2.1.238/v2.1.239のみ(上記、安定性修正)。Cursor/Codex CLI/Aider/Zedとも今日付の具体的発表なし。
- C (WM/OS): Hyprland 0.56.2 (8/5、backport安定化)は既に一般化。CachyOS 8月リリース(Shelly Zig化等)は既出情報の範囲内、当環境(greetd-tuigreet)への影響なし。niri/river/swayとも地殻変動なし。
- D (業務SaaS/AI): freee-mcp・freee Agent Hubとも新規発表なし。一般的な業務効率化SaaSの紹介記事のみで具体的な乗り換え候補なし。
- E (ハード/インフラ): Tailscale修正版確認(上記)。ROCm.AI/ROCm 7.14の追加動きなし(既出継続)。ホームラボ監視・バックアップ系は一般的なまとめ記事のみで新規リリース情報なし。
