# tool-radar 2026-09-17 (期間 2026-09-16 → 2026-09-17)

検出 14件 / 取込価値 0件

## 今日の結論

取り込むべき新規なし。現状維持で十分。Claude Code は v2.1.274 に上がったが内容はメモリ警告・MCP起動待ち時間の環境変数・テレメトリ拡張・Bedrock/Vertex/Foundry向けMCP v2既定化など、いずれも現構成(Max契約・直接API・Bedrock等未使用)には非該当。唯一挙動が変わるのは `/code-review` がチューニング設定のないモデルではサブエージェント艦隊でなく軽量インラインプロンプトを使うようになった点だが、CLAUDE.mdの「maxレビュー艦隊」運用は別のWorkflow経由でこの変更の影響を受けない。A(開発環境)/C(WM・OS)/D(業務SaaS)/E(ハード・インフラ)は軒並み無風で、Hyprland 0.57は進捗20%のまま、ROCmはCachyOSリポジトリで依然rocm-core 7.2.4。

## 推奨アクション (最大3)

(該当なし — 取り込むべきアクションなし)

## 監視継続

- **Claude Code v2.1.274** ⌀ [確認済] — メモリクリティカル時の警告表示、`CLAUDE_CODE_MCP_STARTUP_WAIT_MS`(MCP起動待ち上限)追加、OTelテレメトリ拡張、Bedrock/Vertex/Foundry/テレメトリ無効環境でのMCP v2クライアント既定化。`/code-review`がチューニングなしモデルでは多エージェント生成でなく軽量インラインレビューに変更。いずれも現構成に直撃せず自動適用で追従。
- **Ghostty 1.4.0** ⌀ [推定] — 6ヶ月周期の次マイナー版が9月予定と観測されるが、tip(日次ビルド)止まりで正式リリース未確認。現行安定版は1.3.1のまま。使用中ターミナルのため次回以降も確認継続。
- **freee-mcp v0.35.2** ⌀ [確認済] — 9/16判定から変化なし。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然 `rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし。
- **Tailscale最新版** ⌀ [推定] — 内容(TS-2026-011修正・Go 1.26.6等)は9/9以降と同一、デスクトップ版の実機 `tailscale version` 確認は引き続き宿題。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/17時点で未発表、直近は8月リリースのまま。
- **Codex CLI / Antigravity CLI** ⌀ [確認済] — Codex CLI v0.153.4(GPT-6 Astra既定、9/4既知)、Antigravity CLI v2.14(9/15)ともに通常アップデートの範囲、乗り換え動機なし。
- **Cursor/Codex/Gemini CLI/Antigravity サンドボックスエスケープ報道** ⌀ [推定] — 7月の既出報道が検索結果に再浮上しただけ、新規性なし。
- **Arch Linux DDoSインシデント** ⌀ [推定] — 8月中旬のインシデント記事が検索結果に再浮上しただけ、CachyOS独自リポジトリのため直接影響は引き続き未確認。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazygit/jujutsu/zellij/blink.cmp/telescope.nvim/snacks.nvim/nvim-treesitterとも9/17固有の新着なし。Zed 1.20.2(9/17)はモデルプロバイダのエラー表示修正のみで非導入ツールのため非該当。
- B (AIエージェント): Claude Code v2.1.274のみ、内容は上記の通り非該当中心。Codex CLI/Antigravity CLIは通常更新。MCPエコシステムは8/22ロードマップ以降の新規動向なし。
- C (WM/OS): 動きなし。Hyprland進捗変化なし、niri/river/swayとも9月固有のリリースなし、CachyOSは9月分未発表。
- D (業務SaaS/AI): freee-mcp v0.35.2はパッチ更新のみで非該当。freee/マネーフォワードの会計AI新機能(AIアシスタント・AIおまかせ明細取得等)はいずれも6月・3月発表の既存情報で9月固有の新着ではない。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし。自宅サーバー監視・バックアップ系(Beszel/Duplicati/restic等)も9月固有の新着確認できず。
