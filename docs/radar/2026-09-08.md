# tool-radar 2026-09-08 (期間 2026-09-07 → 2026-09-08)

検出 10件 / 取込価値 0件

## 今日の結論

取り込むべき新規なし。現状維持で十分。期間が1日と短いこともあり、A〜Eいずれのカテゴリでも既出内容を上回る新着は見つからなかった。唯一の新規検出はfreee-mcp v0.35.0(mcp-only APIをfreee Launch/従業員評価のドメイン別に分割)だが、一人法人かつ従業員なしの当環境には該当機能がなく、情報記録に留める。ROCm 10.0のCachyOSリポジトリ反映・Hyprland 0.57マイルストーン・Tailscale最新版はいずれも変化なし(ROCm/Tailscale公式サイトは今回プロキシでブロックされ直接確認できなかったが、検索結果は前日までと同じ値を示した)。

## 推奨アクション (最大3)

なし。

## 監視継続

- **freee-mcp v0.35.0/v0.35.1** ⌀ [確認済] — mcp-only APIサービスをfreee Launch(開業支援)/従業員評価のドメイン別に分割し`service: 'launch'` / `service: 'employee_evaluation'`を個別指定可能に、加えてOpenAPIスキーマ同期のドキュメント更新(v0.35.1)。既存: 当環境は既に法人化済み・従業員なしの一人法人のため、どちらの新ドメインも非該当。
- **Codex CLI v0.153.4 (GPT-6-Astra既定化) / Cursor v08.19.26 / Gemini CLI v0.58.0** ⌀ [確認済] — 競合CLI/エディタのエコシステム追従のみ、乗り換え動機なし。
- **Zellij v0.45.0/v0.45.1 (nested sessions/Kitty graphics/新UI)** ⌀ [確認済] — 新規検出だが8/20-8/28リリースの後追い把握。tmux運用中で乗り換え動機なし、多重化ツール層は移行コスト>>便益が常態のため原則現状維持。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — GitHub直接確認、進捗20%(5件中1件完了)・8/20更新のまま変化なし。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [推定] — 公式`packages.cachyos.org`は今回プロキシでブロックされ直接確認不可、検索結果上は`rocm-core` 7.2.4のまま変化なし。
- **Tailscale最新版** ⌀ [推定] — 公式`tailscale.com`は今回プロキシでブロックされ直接確認不可、検索結果上はv1.102.3から変化なし。実機`tailscale version`確認は宿題のまま。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/8時点で9月分のアナウンス未確認、直近は8月リリース(Shelly Zig化・kernel-manager Rust化)のまま。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/telescope.nvim/snacks.nvim/lazygit/jujutsuとも9月固有の新着なし。Zellij新規検出(上記)も乗り換え動機なし。
- B (AIエージェント): 動きなし。Claude Code公式changelogは9/6付v2.1.263が引き続き最新。Codex CLI/Cursor/Gemini CLIは競合エコシステムの延長のみ。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、niri/river/swayとも9月固有の新着なし、CachyOSは9月分未発表。
- D (業務SaaS/AI): 動きなし。freee-mcp v0.35.0(上記、非該当)以外、freee/マネーフォワード/弥生とも9月固有の乗り換え動機となる新着確認できず。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし(直接確認はプロキシブロックのため検索ベースの推定)。
