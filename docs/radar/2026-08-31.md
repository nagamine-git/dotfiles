# tool-radar 2026-08-31 (期間 2026-08-30 → 2026-08-31)

検出 8件 / 取込価値 2件

## 今日の結論

導入済みMCPプラグイン `freee-mcp` に実利ある動きあり。v0.32.3でRemoteモードOAuthの深刻な可用性バグ(共有client_secretが30日失効・再登録しても復旧不能)が修正され、v0.34.0で固定資産Public API・法人税申告APIが追加された。実機のfreee-mcpバージョン確認を推奨。それ以外のA/C/E領域は8/30付radarから変化なし。

## 推奨アクション (最大3)

1. **freee-mcp v0.32.3以降へのアップデート確認** ★★★ [確認済] — 根拠: RemoteモードのDCR(動的クライアント登録)で発行される`client_secret`が30日で失効し`invalid_client: Client secret has expired`となる不具合があった。claude.ai等ベンダー経由クライアントは登録を全ユーザーで共有するため、失効時に全ユーザーが同時に接続不能になり、再登録してもdedupで同じ失効登録が返り復旧不能という設計上の欠陥。v0.32.3でclient_secretを無期限化し、失効済み登録は自動的に新規発行されるよう修正済み。/ 既存: `enabledPlugins`に`freee-mcp@freee-mcp-marketplace`を導入済み、この可用性リスクが解消される / 移行コスト: 低(バージョン確認・アップデートのみ) / URL: https://github.com/freee/freee-mcp/releases
2. **freee-mcp v0.34.0 固定資産Public API・法人税申告API追加** ★★ [確認済] — 根拠: 固定資産管理・法人税申告書(XML帳票、UTF-8検証・1MiB上限付き)をMCP経由で取得できるAPIが新規追加。一人法人の決算・申告業務をClaude Code経由で扱える「空いてる穴」を埋める可能性 / 既存: freee本体機能の後追いMCP対応、今すぐ使う必要はないが決算期に有用 / 移行コスト: 低(既存プラグインの自動更新範囲) / URL: https://github.com/freee/freee-mcp/blob/main/CHANGELOG.md

## 監視継続

- **Claude Code v2.1.252** ⌀ [確認済] — バグ修正4件(Bashタスク出力swapエラー修正/`.claude/settings.local.json`未作成時の"always allow"保存不具合修正/Remote Control接続劣化時のスタール修正/バックグラウンドタスク巨大失敗出力によるAPIサイズ制限超過修正)。`remoteControlAtStartup: true`環境で地味に有用だが自動適用の安定性改善でアクション不要。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然7.2.4のまま変化なし、反映待ち継続。
- **Tailscale v1.102.3** ⌀ [確認済] — stable最新に変化なし、実機`tailscale version`確認は宿題のまま。
- **Hyprland 0.57** ⌀ [確認済] — マイルストーン進捗20%のまま変化なし。
- **Codex CLI rust-v0.151.0** (8/29) ⌀ — MCPツール発見のグレースピリオド設定化等、未導入・乗り換え動機なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/telescope.nvim/neo-tree/conform.nvim/sheldon/lazygit/jj/miseとも8/30-8/31付の新着なし。
- B (AIエージェント): Claude Code軽微バグ修正4件(上記、アクション不要)。Codex CLI v0.151.0は競合更新のみで乗り換え動機なし。Cursor/Gemini-Antigravity/Aiderは期間内の固有新着なし。
- C (WM/OS): Hyprland/niri/river/sway/CachyOSとも8/30-8/31付の新着なし。
- D (業務SaaS/AI): freee-mcp重要修正・API追加 (上記推奨アクション参照)。freee Agent Hub/顧問先管理AIエージェントは変化なし、税理士事務所向けのため引き続き非該当。
- E (ハード/インフラ): ROCm/Tailscaleとも変化なし。netdata/Uptime Kumaは未導入のため非該当情報のみ。
