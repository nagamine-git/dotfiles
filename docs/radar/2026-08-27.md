# tool-radar 2026-08-27 (期間 2026-08-26 → 2026-08-27)

検出 9件 / 取込価値 3件

## 今日の結論

Claude Code が2バージョン連続 (v2.1.247/248) でこの環境に直接効く機能を出した。`/claude-api cost-optimize` (支出プロファイリング) と agent frontmatterでの `experimental.cacheTtl` 個別指定 (`dot_claude/agents/*.md` に直撃) の2つは移行コストほぼゼロで即採用価値あり。加えてAMDが本日 ROCm 10.0 (7.14→10.0の節目リブランド、ROCm.AIを正式統合) を出荷したが、こちらはdGPU 1枚をデスクトップ描画とROCmで共用する構成上、メジャーバージョン跨ぎでの互換性検証を先にすべき案件。

## 推奨アクション (最大3)

1. **Claude Code v2.1.248 — agent frontmatterの `experimental.cacheTtl`** ★★ [確認済] — サブエージェント単位でprompt cache TTLを個別指定できるようになった。既存: `dot_claude/agents/reviewer.md` / `implementer.md` / `researcher.md` の3エージェントが対象になり、8/25付radarで「監視継続」のまま止まっていた `promptCacheTtl`/`subagentPromptCacheTtl` 設定の粒度不足がここで解消される。移行コスト: 低 — 各SKILL/agentのfrontmatterに1行追加するだけ。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.247 — `/claude-api cost-optimize`** ★★ [確認済] — 支出をプロファイリングしコスト削減レバーを提示する組み込みコマンド。既存: `dot_local/bin/claude-usage-colored.tmpl` / `claude-usage-notify` は使用量の可視化止まりで、削減提案までは行っていない空いてる穴。一人法人で現預金効率を重視する運用方針に直接合致。移行コスト: 低 — アップデート後にコマンドを叩くだけ。URL: https://code.claude.com/docs/en/changelog

3. **ROCm 10.0 (AMD, 本日出荷)** ★★ [確認済] — ROCm 7.14→10.0への節目バージョンで「10周年」リブランド、8/24付radarで★★評価した ROCm.AI/AMD Skills がここで正式に統合された。既存: 現行 ROCm 7.14 運用中。移行コスト: 中〜高 — dGPU 1枚をHyprlandデスクトップ描画とROCm推論で共用する構成のため、メジャーバージョン跨ぎでのドライバ/カーネルモジュール互換性を検証してからでないと描画側に波及するリスクがある。CachyOSリポジトリへの反映を待ってからの追従が無難。URL: https://rocm.blogs.amd.com/ecosystems-and-partners/rocm-x-blog/README.html

## 監視継続

- **Cursor** ★ [推定] — SpaceXによる$60B買収報道 (Q3完了予定) は状況変化なし。乗り換え動機なし。
- **freee Agent Hub / freee顧問先管理AIエージェント** ⌀ — 明日8/28正式提供開始。引き続き税理士事務所向けのため非該当。
- **Tailscale TS-2026-009/010/011** ⌀ — 修正版番号 (v1.98.9以降/v1.102.3) 判明済みだが実機の `tailscale version` 確認は依然未実施。
- **MCP仕様2026-07-28 (ステートレス化) / 新ロードマップ** ⌀ — 8/22のロードマップ発表以降、状況変化なし。freee-mcpの対応状況は引き続き不明。
- **Codex CLI / Cursor Cloud Agents / Meta Muse Code** ⌀ — いずれも既出の延長線上の機能追加のみ、乗り換え動機なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。telescope.nvim/blink.cmp/nvim-treesitter/lazygit/jujutsuに今日付の破壊的変更や新着なし。
- B (AIエージェント): Claude Code v2.1.247/248 (上記、実利アクション2件) が中心。Codex CLI・Cursorは既出機能の延長のみ。
- C (WM/OS): Hyprland 0.56.2が最新のまま新版なし。niri/river/swayにも今日付の新着なし。CachyOSも8月リリースから変化なし。
- D (業務SaaS/AI): freee Agent Hub正式開始 (8/28) が1日後に迫るのみ、内容変化なし。
- E (ハード/インフラ): ROCm 10.0出荷 (上記、要検証の上で追従) が唯一の実質的な動き。Tailscaleは実機確認待ちのまま。バックアップ系 (restic/kopia/borg) に今日付のリリースなし。
