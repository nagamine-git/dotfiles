# tool-radar 2026-08-25 (期間 2026-08-24 → 2026-08-25)

検出 6件 / 取込価値 1件

## 今日の結論

Claude Code v2.1.245 が、glibc 2.44 環境 (Arch Linux・**CachyOS**・Fedora Rawhide) での起動時クラッシュを修正した。Arch のコア glibc パッケージは既に 2.44 系が配布されており、ローリングリリース運用の am5-itx機で `pacman -Syu` 済みなら、日常ツールである Claude Code CLI が更新前は起動不能になっていた可能性がある。実機でのバージョン確認と更新を推奨する、今日いちばんの実利アクション。他は既出情報の範囲内で新規性なし。

## 推奨アクション (最大3)

1. **Claude Code v2.1.245** ★★★ [確認済] — 2026-08-25付で、glibc 2.44 を配布する Linux ディストリ (Arch Linux・CachyOS・Fedora Rawhide) 名指しで、起動時クラッシュを修正したパッチが出た。既存: am5-itx機はCachyOS (Archベース) のローリングリリースで、Arch coreリポジトリのglibcは既に2.44系。日常的に使う Claude Code CLI 本体が対象に含まれており、これは「新ツール導入」ではなく「既存の主力ツールが実機で壊れている可能性がある」の部類。移行コスト: 低 — 通常の自動更新 (auto-update) で v2.1.245 以降になっていれば対応済み。念のため実機で `claude --version` を確認し、v2.1.245未満かつ起動不安定を感じたことがあれば手動更新を。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.243 (8/24)** ⌀ [確認済] — `/usage` への Loops内訳追加、`modelPicker` 設定 (モデル選択肢のカスタムキュレーション)、`promptCacheTtl`/`subagentPromptCacheTtl` 設定など。`dot_claude/CLAUDE.md` のモデル配車則 (Fable週次枠管理・subagent別モデル指定) を設定ファイル側で補強できる可能性はあるが、緊急性なし。次回 dot_claude/ 更新時に modelPicker 導入を検討する価値はある。
- MCP仕様 2026-07-28 (ステートレス化) ⌀ [確認済] — Anthropic公式ブログでClaudeへの正式導入が進行中と確認したが、freee-mcp側の対応状況は引き続き不明。実害が出るまで監視継続。
- Tailscale TS-2026-009/010/011 ⌀ [確認済] — 3件とも v1.102系で修正済みと確認できたが、実機の `tailscale version` 確認はまだ未実施のまま。ローリングリリース運用なら通常更新で解消見込み。
- CachyOS 8月リリース / Hyprland 0.56.2 / lazygit v0.64.1 / freee Agent Hub (8/28正式開始) ⌀ — いずれも8/24以前に既出の内容から変化なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。nvim(lazy.nvim/blink.cmp/telescope)・sheldon・tmux plugin群・mise・lazygit・jujutsuとも今日付の新着なし。
- B (AIエージェント): Claude Code v2.1.245 (上記、実利アクション) が唯一の新規動意。v2.1.243の機能群は監視継続。Cursor (価格改定確認のみ)・Codex CLI (rust-v0.149.0)・Aiderとも通常更新の範囲内で乗り換え動機なし。
- C (WM/OS): Hyprland・CachyOSとも8/22-24付で既出の内容から進展なし。niri/river/swayに地殻変動なし。
- D (業務SaaS/AI): freee Agent Hub正式開始日 (8/28) が3日後に迫るのみ、内容変化なし。引き続き税理士事務所向けのため非該当。
- E (ハード/インフラ): Tailscale監視事項は状況変化なし。ROCm.AIは「8月中に提供開始」の位置づけ再確認のみ (amd-skillsは8/24既出)。Uptime Kuma/Restic/Kopia/Beszelに今日付の新規リリースなし。
