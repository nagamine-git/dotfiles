# tool-radar 2026-08-30 (期間 2026-08-29 → 2026-08-30)

検出 11件 / 取込価値 0件

## 今日の結論

取り込むべき新規なし。現状維持で十分。期間1日と短く、A〜Eいずれのカテゴリでも8/29付radarを上回る新着は見つからなかった。Claude Codeにマイナーな挙動改善 (`/add-dir`のターン中実行、`/goal`のエラー時自動クリア、クロスセッションメッセージング周りの安定化) はあったが、いずれも自動適用される地味な修正で、ユーザー側の設定変更やアクションを要するものではない。

## 推奨アクション (最大3)

なし。

## 監視継続

- **Claude Code — 直近パッチのマイナー改善群** ⌀ [確認済] — `/add-dir`をClaude作業中でも実行可能に、`/goal`が復旧不能エラーでターン終了時に自動クリアされるよう変更、バックグラウンドタスクが30分以上`/goal`を待たせている場合の確認強化、`claude setup-token`の余剰引数拒否、Escapeキーがマウス選択を消さないよう修正、Remote Control使用中セッションのresume時通知改善など。当環境は`remoteControlAtStartup: true`だが、いずれも自動適用の安定性改善でアクション不要。
- **ROCm 10.0 の CachyOS リポジトリ反映状況** ⌀ [確認済] — `packages.cachyos.org`を再確認したが`rocm-core`は依然7.2.4 (6/2ビルド) のまま。反映待ち継続。
- **Tailscale 実機バージョン確認** ⌀ [確認済] — stable最新は引き続きv1.102.3 (8/19)、8月の新版なし。TS-2026-009/010/011はいずれも修正済みバージョンに含まれる想定。実機での`tailscale version`確認は宿題のまま。
- **Hyprland 0.57** ⌀ [確認済] — GitHubマイルストーンは8/20時点で進捗20%、8/29時点から状況変化なし。正式リリースの見通しなし。
- **freee Agent Hub / 顧問先管理AIエージェント** ⌀ — 8/28正式提供開始・9/15エージェント提供開始の内容を再確認しただけ、変化なし、税理士事務所向けのため引き続き非該当。
- **MCP仕様2026-07-28 / 新ロードマップ** ⌀ [確認済] — 8/22公開のロードマップ (server-initiated events、Tasks拡張の仕様化など) 以降、具体的な追加リリースなし。freee-mcpのフル対応可否は未確認のまま。
- **Claude Code Plugin Marketplace統計 (skills 4391件/MCPサーバ916件超)・GitKraken MCPプラグイン正式化** ⌀ [確認済] — 数字が微増しただけ、GitKrakenは当環境で未使用のため非該当。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/telescope.nvim/neo-tree/lazygit/jujutsuとも今日付の新着・破壊的変更なし。
- B (AIエージェント): Claude Codeのマイナー安定性改善 (上記、アクション不要) のみ。Cursor/Codex CLI/Gemini CLIは既出方向性の延長のみで乗り換え動機なし。
- C (WM/OS): Hyprland 0.57は依然開発中 (進捗20%)。niri/river/swayも今日付の新着なし。CachyOSは8月リリース内容の反復のみ。
- D (業務SaaS/AI): freee Agent Hub正式化は8/28分の再確認のみ、動きなし。
- E (ハード/インフラ): ROCm/Tailscaleとも変化なし。Uptime Kuma/netdataは未導入のため非該当情報のみ。
