# tool-radar 2026-09-03 (期間 2026-09-02 → 2026-09-03)

検出 9件 / 取込価値 3件

## 今日の結論

Claude Code v2.1.259 に、この環境の構成に直接効く修正が3件同時に来た。(1) auto mode がコマンド/skillのfrontmatterでモデル指定がある場合に非対応モデルでターンを実行してしまうバグが修正され、これは `dot_claude/agents/reviewer.md`(opus)/`implementer.md`(sonnet)/`researcher.md`(sonnet) が全てfrontmatterでモデルを明示し、かつ `defaultMode: "auto"` を全面採用している当環境に直撃する。(2) 並行セッションが互いの `~/.claude.json` の変更を無言で上書きし合うバグが修正され、`remoteControlAtStartup: true` + 本radarのようなスケジュール実行セッション + 対話セッションが同一マシン上で並行しうる運用に実利がある。(3) Remote Controlセッションでの Stop がバックグラウンドagent/workflowを実際には止めていなかったバグが修正され、`remoteControlAtStartup: true` + `skipWorkflowUsageWarning: true`(workflow運用を示唆)の構成に効く。それ以外のA(開発環境)/C(WM/OS)/D(業務SaaS)/E(ハード/インフラ)は無風。

## 推奨アクション (最大3)

1. **Claude Code v2.1.259 — auto modeがコマンド/skill frontmatter指定モデルを無視して非対応モデルでターン実行するバグを修正** ★★★ [確認済] — 根拠: コマンドやskillのfrontmatterで`model`を指定していても、auto modeが対応していないモデルでターンを実行してしまう不具合があった。既存: `dot_claude/agents/reviewer.md`(`model: opus`)/`implementer.md`(`model: sonnet`)/`researcher.md`(`model: sonnet`)の3エージェント全てがfrontmatterで明示的にモデル指定しており、`dot_claude/CLAUDE.md`の「モデル配車則」も"Agent/Workflowのsubagentは必ずmodelを明示する"と定めている。`defaultMode: "auto"`環境ではこの明示指定が地味に無視されていた可能性がある。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.259 — 並行セッションが `~/.claude.json` の変更を互いに無言で上書きするバグを修正** ★★ [確認済] — 根拠: 複数セッションが並行動作すると、workspace trustのリセットやMCP/プロジェクト状態の消失が起きていた。既存: `remoteControlAtStartup: true`で対話セッションをリモートからも操作可能にしており、かつ本radar自体が毎朝スケジュール実行される独立セッションであるため、対話セッションと同時に走った場合に設定が無言で失われるリスクがあった。移行コスト: 低 — 自動適用、アクション不要。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.259 — Remote Controlセッションで Stop がバックグラウンドagent/workflowを実際には停止していなかったバグを修正** ★★ [確認済] — 根拠: リモートから「停止」を指示してもバックグラウンドで動いているagentやworkflowが動き続けていた。既存: `remoteControlAtStartup: true` かつ `skipWorkflowUsageWarning: true`(workflow機能を日常的に使う前提の設定)の組み合わせで、リモート停止指示が効かず意図せぬ課金・動作継続が起きうる穴だった。移行コスト: 低 — 自動適用、次回リモート停止操作時に効果を確認するだけ。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.259 — その他の修正・新機能** ⌀ [確認済] — `managedMcpServers`(組織向け、一人法人には非該当)、`--permission-prompts none`(無人ホスト向けヘッドレスフラグ、既存の`skipDangerousModePermissionPrompt`/`skipAutoPermissionPrompt`と役割が重なる可能性、緊急性なし)、GitLab `glab` コマンド認識(GitHub運用のため非該当)、Bashの`Read()` deny rule がオプション値・git引数・複合コマンドで効かないバグ修正(当環境の`injection-guard.sh`フックは独自正規表現マッチのため直接の影響は限定的)。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — 依然`rocm-core` 7.2.4のまま、反映待ち継続。
- **Tailscale最新版** ⌀ [確認済] — TailscaleUp基調講演(8/26)由来のAperture GA・PAM betaは既に8/28分で把握済み、9月分の新規バージョンや脆弱性情報なし。実機`tailscale version`確認は宿題のまま。
- **Hyprland 0.57** ⌀ [確認済] — マイルストーン進捗20%のまま変化なし。
- **freee-mcp** ⌀ [確認済] — v0.34.2(9/1)以降の新規リリースなし。
- **lazygit** ⌀ [確認済] — v0.64.1(8/12)が最新のまま変化なし、config未使用のため影響なし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。lazygit/jujutsu/nvimエコシステム(blink.cmp/neo-tree/flash.nvim等)/sheldon/tmuxプラグイン群とも期間内の固有新着なし。
- B (AIエージェント): Claude Code v2.1.259(上記、実利アクション3件+監視事項)が中心。Codex CLI/Cursor/Gemini系(Antigravity)は既出動向の延長のみで乗り換え動機なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし。Arch Linux 2026.09.01 ISO(kernel 7.2.2/glibc 2.44)は9/2分で既に把握済みの内容の反復。
- D (業務SaaS/AI): 動きなし。freee-mcp/freee Agent Hubとも期間内の新規動意なし。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし。
