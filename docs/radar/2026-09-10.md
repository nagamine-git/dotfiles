# tool-radar 2026-09-10 (期間 2026-09-09 → 2026-09-10)

検出 12件 / 取込価値 3件

## 今日の結論

Claude Code v2.1.268 (9/10) に、現環境の権限モデル・プラグイン運用に直接効くセキュリティ修正が複数まとまって来た。特に「Bashのdeny規則がsymlinkパスの綴りを無視する」「同一行に`env -C`/`eval`等があるとRead/Editのdeny規則が効かない」の2件は、`private_settings.json.tmpl`の`permissions.deny`(`git push --force`系・`gh repo create/delete`・`npm/yarn/pnpm/cargo publish`・**`op item`/`op read`/`op signin`/`op document`= 1Password秘密情報保護**)がまさに回避され得た穴を塞ぐ。あわせてプラグイン/マーケットプレイスのgitソースURLからのトークン漏洩、MCP設定の`${VAR}`解決後シークレットが`/mcp`・`/plugin`表示で露出するバグも修正されており、`freee-mcp-marketplace`(github)・Slack MCP(oauth clientId)を使う当環境に実利がある。それ以外のA(開発環境)/C(WM・OS)/D(業務SaaS)/E(ハード・インフラ)は無風。

## 推奨アクション (最大3)

1. **Claude Code v2.1.268 — 権限deny規則のsymlink/env経由バイパス修正 (2件)** ★★★ [確認済] — (a) `/etc`・`/tmp`・`/var`(macOS)・`/bin`(Linux)等のsymlinkディレクトリに対し、パスを実体側の綴りで与えるとdeny/ask規則が適用されない不具合、および symlink綴りで書いたBashのdeny規則がコマンドから無視される不具合を修正。(b) `env -C`・`eval`等パーミッションチェッカーが解析できないコマンドが同一行にあると、Read/Editのdeny規則が効かないケースを修正。既存: `dot_claude/private_settings.json.tmpl`の`permissions.deny`に`git push --force*`系、`gh repo create/delete`、`npm/yarn/pnpm/cargo publish`、`sendmail`系、そして`op item*`/`op read*`/`op signin*`/`op document*`(1Password関連コマンドの秘密情報保護)を設定済み — これらのdeny規則が回避され得た経路を塞ぐ直接的な安全性向上。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.268 — プラグイン/マーケットプレイス・MCP設定のシークレット露出修正** ★★ [確認済] — プラグイン/マーケットプレイスのエラーがgitソースURLに含まれるトークンやパスワードを表示してしまう不具合、および`/mcp`・`/plugin`のサーバ詳細・`claude mcp list/get`・MCPログインエラーがMCP設定の`${VAR}`プレースホルダーを解決した後のシークレットを表示してしまう不具合を修正。既存: `dot_claude/private_settings.json.tmpl`で`extraKnownMarketplaces`に`github:freee/freee-mcp`(freee-mcp-marketplace)を登録し有効化中、`mcpServers`にSlackのoauth clientIdを含む設定を保持 — エラー表示・一覧表示経路での秘密情報露出リスクを直接減らす。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.268 — 未信頼フォルダの同名agentファイルを拾ってしまう再生成バグ修正** ★★ [確認済] — in-processチームメイトが再起動(respawn)する際、信頼していないフォルダにある同名のagentファイルからツールやシステムプロンプトを誤って引き継いでしまう不具合を修正。既存: `dot_claude/agents/{implementer,researcher,reviewer}.md`の3エージェント運用でagentファイルの信頼境界に依存しており、直接関係する安全性修正。移行コスト: 低 — 通常アップデートで自動適用。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **TodoWrite/TaskCreate系ツールの対象モデル判明** ⌀ [確認済] [再掲:変化あり・監視終了] — v2.1.268でタスク管理系ツール(TaskCreate/Get/Update/List, TodoWrite)が「Claude 3.x, Opus 4.0–4.7, Sonnet 4.0–4.6, Haiku 4.5」でのみ既定提供に絞られ、それ以外(Opus 5等の現行モデル群を含む)では`CLAUDE_CODE_ENABLE_TODO_TOOLS=1`が必要と判明。8/15から継続監視していた「TodoWrite既定オフ」の具体像がようやく判明したが、`dot_claude/skills/daily-focus/SKILL.md`・`dot_claude/skills/dev-radar/SKILL.md`ともTodoWrite/TaskCreateを一切参照しておらず実害なしと確認できたため、この監視事項はクローズする。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — `packages.cachyos.org`で依然`rocm-core` 7.2.4のまま、反映待ち継続。
- **Hyprland 0.57マイルストーン** ⌀ [確認済] — 進捗20%(5件中1件完了)のまま変化なし、安定版は引き続き0.56.2。
- **Tailscale最新版** ⌀ [推定] — 公式サイト直接確認は今回も不可。TS-2026-011(host-scoped IPv4/4via6)修正・Go 1.26.6更新の言及自体は9/9時点と同一内容で新規性なし、実機`tailscale version`確認は宿題のまま。
- **freee-mcp** ⌀ [確認済] — v0.35.1(9/8)が最新のまま、新規リリースなし。
- **CachyOS 9月リリース** ⌀ [確認済] — 9/10時点で未発表。
- **マネーフォワード/弥生/freee AI機能動向** ⌀ [確認済] — 9月固有の新着・乗り換え動機となる具体的発表は確認できず。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。blink.cmp/snacks.nvim/telescope.nvim/lazygit/jujutsu/sheldon/tmuxプラグイン群とも9月固有の新着なし。
- B (AIエージェント): Claude Code v2.1.268に実利あるセキュリティ修正3系統あり(上記参照)。Cursor/Codex CLI(v0.153.4のまま)/Antigravity CLI/Aiderは既出動向の延長のみで9/10固有の新着なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし、Arch Linux 2026.09.01 ISO(kernel 7.2.2)は9/2分の既出情報の再確認のみ、CachyOSは9月分未発表。
- D (業務SaaS/AI): 動きなし。freee/マネーフォワード/弥生いずれも9月固有の乗り換え動機となる新着確認できず。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし(Tailscaleは直接確認不可のため推定)。
