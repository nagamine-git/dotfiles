# tool-radar 2026-08-15 (期間 2026-07-16 → 2026-08-15)

検出 23件 / 取込価値 3件

## 今日の結論

新規ツールとしての「取り込み」は無し。今日の本命は**確認作業**: MCP仕様の破壊的変更が freee-mcp 連携に波及していないか、Tailscale の既知脆弱性修正が適用済みか、Claude Code の権限モード/Todoツール既定変更が既存 skills に影響しないか、の3点をチェックすべき日。

## 推奨アクション (最大3)

1. **MCP仕様 2026-07-28 (ステートレス化)** ★★ [確認済(仕様)/不明(freee-mcp対応)] — MCP史上最大級の破壊的変更でセッション管理を撤廃。Claude Code から freee-mcp を使った会計操作連携が対象。既存: 導入済み freee-mcp プラグインへの影響有無を `github.com/freee/freee-mcp` の Releases で確認。移行コスト: 低 (対応版が出ていれば更新するだけ、未対応でも現状動いていれば急ぐ必要なし)。URL: https://gihyo.jp/article/2026/07/mcp-spec-2026-07-28

2. **Tailscale SSH 脆弱性修正 (TS-2026-009/010)** ★★ [確認済] — root権限バイパスの脆弱性が v1.98.9 (7/14) で修正済み。Tailscale を SSH 経由アクセス・exit-node に日常的に使用中 (waybar連携あり)。既存: バージョン確認のみで完結する軽微な対応。移行コスト: 低 (`tailscale version` 確認 → 未満なら `pacman -Syu`)。URL: https://news.ycombinator.com/item?id=48915004

3. **Claude Code 8月アップデート一式 (auto mode既定化 / TodoWrite既定オフ / Slack MCP OAuth修正)** ★★ [確認済] — 8/14 に新規セッションの権限モードが auto に既定変更、同日 Opus 5/Sonnet 5/Fable 5 で TodoWrite 等が既定無効化 (`CLAUDE_CODE_ENABLE_TODO_TOOLS=1`で復元可)。既存: `defaultMode`未設定なら挙動変化の可能性、`daily-focus` skill が Todo系ツールに依存していれば要検証。Slack MCP の OAuth 修正 (v2.1.231) は導入済み Slack MCP 連携に直接該当。移行コスト: 低 (設定確認+動作確認のみ)。URL: https://code.claude.com/docs/en/whats-new/2026-w32

## 監視継続

- mise v2026.8.2 宣言的システムブートストラップ ⌀ — nix/home-manager領域に接近する新機能だが opt-in。dotfiles基盤の移行は便益 << コストなので様子見。
- lazygit v0.64.0 (`git.pagers`→`git.diffRenderers`改名の破壊的変更) ⌀ — 当リポジトリに lazygit config 未管理のため影響なし。通常アップデートで可。
- jujutsu (jj) v0.44.0 タグ追跡stable化 ⌀ — 未導入。lazygit運用で不満なし、乗り換え動機なし。
- Arch AUR 悪意パッケージ大量発生 (7/25-8/1) ⌀ — pkglist.txt に yay/paru の記載なし (AURヘルパー未導入と推定)。該当なしだが念のため運用実態を確認する価値あり。
- CachyOS 8月リリース (greeter が SDDM→noctalia-greeter) ⌀ — 本環境は greetd-tuigreet 使用でSDDM非該当、影響なし。
- Hyprland 0.56/0.56.2 ⌀ — 破壊的変更なしの通常アップデート。進行中の Lua 移行作業はそのまま継続でよい。
- ROCm 7.14 正式版 (HIP Execution Contexts でタスク単位のGPU分割) ⌀ — dGPU共用環境に地味に有用だが緊急性なし。通常アップデートで追従。
- ROCm.AI (Claude Code/Cursor向けAMD Skills) ⌀ — 8月ロールアウト中、様子見。
- chrome-devtools-mcp v1.7.0 ⌀ — 導入済み (zeed-browser)。通常アップデートで追従すればよい。
- Cursor Router (モデル自動選択でコスト60%減主張) ⌀ — Claude Code に同等機能なし。乗り換え動機にはならない、動向として記録のみ。
- Notion AI利用上限導入 (8/3) ⌀ — 既存ユーザーへの制限強化。移行検討には値しない。
- 弥生「記帳代行AI」β版・労務Next単体提供 ⌀ — freee からの乗り換えコスト(仕訳移行・freee-mcp連携再構築)が便益を大きく上回る。

## カテゴリ別カバレッジ

- A (開発環境): mise宣言的ブートストラップ・lazygit破壊的変更・jujutsu安定化と動きあり、いずれも現環境に緊急対応不要。
- B (AIエージェント): Claude Code側で複数アップデート (権限モード既定変更・Todoツール既定オフ・Slack MCP修正)、chrome-devtools-mcp更新。要確認3点の中心。
- C (WM/OS): Hyprland通常更新、CachyOS8月リリース、Arch AUR侵害インシデント (本環境は影響軽微)。
- D (業務SaaS): MCP仕様破壊的変更が最重要、他は freee 一強を崩す動きなし。
- E (インフラ): ROCm 7.14正式版、Tailscale脆弱性修正が唯一の即応候補。

