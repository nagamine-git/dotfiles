# tool-radar 2026-09-04 (期間 2026-09-03 → 2026-09-04)

検出 11件 / 取込価値 3件

## 今日の結論

Claude Code v2.1.260/v2.1.261 に、この環境の運用実態(Fable週次枠管理・cacheTtl効果検証・3スキル運用)に直接効く変更が3件来た。とくに v2.1.260 の Fable 5.1 プロンプトキャッシュ不具合修正は、ツール呼び出し後のコンテキストがキャッシュされず毎回再送されていたバグの修正で、`CLAUDE.md`のモデル配車則が前提とする「Fable週次枠は貴重なので節約する」という運用コストの実態に直結する。それ以外(C: WM/OS, D: 業務SaaS, E: ハード/インフラ)は無風で、Hyprland 0.57/ROCm 10.0のCachyOS反映/Tailscale最新版/freee-mcpいずれも8月末〜9月頭時点から状況変化なし。

## 推奨アクション (最大3)

1. **Claude Code v2.1.260 — Fable 5.1 のプロンプトキャッシュ不具合修正** ★★★ [確認済] — 根拠: Fable 5.1セッションでツール結果の後のコンテキストがキャッシュ対象にならず、毎ターン非キャッシュで再送されていた不具合を修正。既存: `dot_claude/CLAUDE.md`の「モデル配車則」は"Fable週次枠は最大50%・追加枠なし・週次枠>80%でOpusへ自主降格"と定めており、Fable利用のコスト効率がそのまま週次枠の消費速度に直結する構造。ツール呼び出しを伴う実装寄りのFableセッションほど、このバグで実質的に枠を余計に消費していた可能性がある。移行コスト: 低 — 通常アップデートで自動適用、次回Fableセッションで`/cost`のキャッシュ内訳を確認すれば効果を検証できる。URL: https://code.claude.com/docs/en/changelog

2. **Claude Code v2.1.261 — `/skill-doctor` 追加** ★★ [確認済] — 根拠: 未使用スキルとそのコンテキストコストを可視化し、剪定を支援する新コマンド。既存: `dot_claude/skills/`に`daily-focus`・`dev-radar`(本radar自身)・`session-title-refresh`の3スキルが常駐しており、これまで各スキルが実際にどれだけコンテキストを食っているか確認する手段がなかった空いてる穴。移行コスト: 低 — 次回セッションで`/skill-doctor`を実行するだけ。URL: https://code.claude.com/docs/en/changelog

3. **Claude Code v2.1.260 — `/cost`・ステータスラインへのキャッシュミス原因表示追加** ★★ [確認済] — 根拠: prompt-cacheミスの推定原因(ツール変更・システムプロンプト変更・TTL切れ)が`/cost`とステータスラインに表示されるようになった。既存: 8/27付radarで採用を推奨した agent frontmatter の `experimental.cacheTtl`(`dot_claude/agents/reviewer.md`/`implementer.md`/`researcher.md`)や8/28付で追加された`/cost`のキャッシュ内訳表示に続き、「なぜミスしたか」まで踏み込んで確認できるようになった。移行コスト: 低 — 次回セッションで確認するだけ。URL: https://code.claude.com/docs/en/changelog

## 監視継続

- **Claude Code v2.1.260/261 — その他の修正** ⌀ [確認済] — プラグインフックのロード失敗後にセッション全体でモデル切替がブロックされたままになる不具合の修正(`freee-mcp@freee-mcp-marketplace`含む5プラグイン+PreToolUse/Stopフック常用構成に間接的に有用)、claude.aiからのプラグイン同期で重複/消失が起きる不具合の修正、Remote Controlの権限モード表示が古いまま/接続デバイスからの停止でセッションが固まる不具合の修正(`remoteControlAtStartup: true`構成に有用)。いずれも通常アップデートで自動適用、個別の設定変更は不要。
- **ROCm 10.0 CachyOSリポジトリ反映状況** ⌀ [確認済] — `packages.cachyos.org`で`rocm-core`は依然7.2.4 (ビルド日6/2)のまま、反映待ち継続。
- **Hyprland 0.57** ⌀ [確認済] — マイルストーン進捗20%のまま変化なし、未リリース。
- **Tailscale最新版** ⌀ [確認済] — 9月分の具体的リリースノートは未確認、8月末のTailscaleUp由来のPAM beta/Aperture GAから進展なし。実機`tailscale version`確認は宿題のまま。
- **freee-mcp** ⌀ [確認済] — v0.34.2(9/1)以降の新規リリースなし。
- **Cursor / Codex CLI 動向** ⌀ [推定] — Cursorの自己ホスト型実行マシン・エージェント専用ワークスペース、Codex CLIのLinux版デスクトップアプリプレビューなど競合CLI/エディタ側の機能拡張が報じられているが、正確な発表日は特定できず、乗り換え動機もなし。情報としてのみ記録。
- **blink.cmp / snacks.nvim / telescope.nvim** ⌀ [確認済] — blink.cmp最新版は4/4付v1.10.2のままで9月の新着なし。「Telescope→snacks pickerへの移行」は一般論としては見かけるが日付の特定できる新着ではなく、現状両方導入済みの当環境で移行を急ぐ理由もなし。

## カテゴリ別カバレッジ

- A (開発環境): 動きなし。nvim(blink.cmp/telescope/snacks.nvim)/lazygit(v0.64.1のまま)/jujutsu/sheldon/tmuxプラグイン群とも期間内の固有新着なし。
- B (AIエージェント): Claude Code v2.1.260/261(上記、実利アクション3件+監視4件)が中心。Cursor/Codex CLIは競合エコシステムの延長のみで乗り換え動機なし。
- C (WM/OS): 動きなし。Hyprland 0.57進捗変化なし。CachyOS/Archはローリング更新の反復のみで9月固有のアナウンスなし。niri/river/swayにも9月固有の動きなし。
- D (業務SaaS/AI): 動きなし。freee関連(Agent Hub/顧問先管理AIエージェント/一般会計プラン)とも9月固有の新着確認できず、既出内容から進展なし。
- E (ハード/インフラ): 動きなし。ROCm/Tailscaleとも実質的な変化なし。
