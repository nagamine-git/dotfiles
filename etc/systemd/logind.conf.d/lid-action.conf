[Login]
HandleLidSwitch=suspend
# AC 給電中の蓋閉じは suspend しない (ignore)。WiFi ノートは外から WoL 起床
# できないため、自宅で挿しっぱなし+蓋閉じでも Tailscale 常時可達を保つ。
# 画面は hypridle の dpms off で消える。バッテリー時 (HandleLidSwitch) は従来どおり suspend。
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
